---
phase: 02-integration-core
plan: 01
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - custom_components/party_dispenser/config_flow.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
must_haves:
  truths:
    - "A user can open the Home Assistant Integrations page, click 'Add Integration', search 'Party Dispenser', and see a form with fields Host, Port, API token, Use HTTPS, Polling interval"
    - "When a user submits the form with valid host/port/JWT, the config flow calls GET {scheme}://{host}:{port}/recipes with Authorization: Bearer <jwt>, and if the backend returns 200 with a JSON list, creates a config entry titled 'Party Dispenser ({host}:{port})'"
    - "When the backend returns 401/403, the form re-renders with base error 'invalid_auth' (translated to 'The API token was rejected.')"
    - "When the backend is unreachable (network failure / timeout), the form re-renders with base error 'cannot_connect' (translated to 'Failed to connect to the backend.')"
    - "When the backend returns a non-JSON or malformed response, the form re-renders with base error 'invalid_response'"
    - "Submitting the SAME host+port twice aborts the second attempt with reason 'already_configured' (unique_id is f'{host}:{port}')"
    - "Opening the integration card's 'Configure' link launches an OptionsFlow showing API token, Use HTTPS, Polling interval — the user can rotate the JWT and save without re-adding the integration"
    - "The JWT field renders with type=password (cleartext masked in UI)"
    - "The Polling interval field renders as a NumberSelector with min=5, max=600, unit_of_measurement='s'"
  artifacts:
    - path: "custom_components/party_dispenser/config_flow.py"
      provides: "PartyDispenserConfigFlow (ConfigFlow subclass) + OptionsFlowHandler (OptionsFlow subclass, 2025.12+ pattern with NO __init__ args)"
      contains: "class PartyDispenserConfigFlow(ConfigFlow, domain=DOMAIN) AND class OptionsFlowHandler(OptionsFlow) AND async def async_step_user AND async def async_step_init"
  key_links:
    - from: "custom_components/party_dispenser/config_flow.py"
      to: "custom_components/party_dispenser/api.py"
      via: "import of PartyDispenserApiClient, PartyDispenserAuthError, PartyDispenserConnectionError, PartyDispenserProtocolError, PartyDispenserError"
      pattern: "from .api import.*PartyDispenserApiClient"
    - from: "custom_components/party_dispenser/config_flow.py"
      to: "homeassistant.helpers.aiohttp_client"
      via: "import of async_get_clientsession for the shared HA aiohttp.ClientSession"
      pattern: "from homeassistant.helpers.aiohttp_client import async_get_clientsession"
    - from: "custom_components/party_dispenser/config_flow.py"
      to: "custom_components/party_dispenser/const.py"
      via: "import of CONF_HOST, CONF_PORT, CONF_JWT, CONF_USE_TLS, CONF_SCAN_INTERVAL, DEFAULT_PORT, DEFAULT_SCAN_INTERVAL, MIN_SCAN_INTERVAL, MAX_SCAN_INTERVAL, DOMAIN, LOGGER"
      pattern: "from .const import .*CONF_HOST"
---

<objective>
Create the user-facing config flow for Party Dispenser: a single `async_step_user` step collecting host/port/JWT/TLS/scan_interval, validating connectivity against the real backend via `GET /recipes` before saving, and an `OptionsFlowHandler` that lets the user rotate the JWT and change the polling interval without re-adding the integration. Honors CFG-01, CFG-02, CFG-03.

Purpose: Flip Phase 1's `config_flow: false` scaffold into a real UI-driven onboarding flow so Home Assistant users can install Party Dispenser entirely through the HA Integrations page — no YAML edits, no secrets.yaml wrangling.

Output: One new file — `custom_components/party_dispenser/config_flow.py` — that registers with HA (implicitly, via `config_flow: true` in manifest.json, already flipped by 02-02) and provides the complete onboarding + options experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jamaze/projects/hacs-integration-pd/.planning/PROJECT.md
@/Users/jamaze/projects/hacs-integration-pd/.planning/ROADMAP.md
@/Users/jamaze/projects/hacs-integration-pd/.planning/REQUIREMENTS.md
@/Users/jamaze/projects/hacs-integration-pd/.planning/phases/02-integration-core/02-CONTEXT.md
@/Users/jamaze/projects/hacs-integration-pd/.planning/phases/02-integration-core/02-RESEARCH.md
@/Users/jamaze/projects/hacs-integration-pd/.planning/phases/02-integration-core/02-VALIDATION.md

<!-- Plan 02-02 lands FIRST. It creates api.py (exception classes + PartyDispenserApiClient) AND extends const.py with CONF_HOST/CONF_PORT/CONF_JWT/etc. This plan consumes both. -->
@/Users/jamaze/projects/hacs-integration-pd/.planning/phases/02-integration-core/02-02-PLAN.md

<interfaces>
<!-- Contracts defined by 02-02 that this plan consumes. Executor should use these directly — no codebase exploration needed. -->

From custom_components/party_dispenser/api.py (created by 02-02):
```python
class PartyDispenserError(Exception): ...
class PartyDispenserAuthError(PartyDispenserError): ...                # 401/403
class PartyDispenserConnectionError(PartyDispenserError): ...           # network / timeout
class PartyDispenserProtocolError(PartyDispenserError): ...             # malformed / 4xx / 5xx

class PartyDispenserApiClient:
    def __init__(
        self,
        base_url: str,
        jwt: str,
        session: aiohttp.ClientSession,   # INJECTED — always pass async_get_clientsession(hass)
        timeout: int = DEFAULT_TIMEOUT_SECONDS,
    ) -> None: ...

    async def list_recipes(self) -> list[Recipe]: ...
    async def list_queue(self) -> list[QueueItem]: ...
    async def order_from_recipe(self, recipe_id: str, session_uid: str = ...) -> OrderResult: ...
    async def cancel_order(self, order_id: str, session_uid: str = ...) -> None: ...
```

From custom_components/party_dispenser/const.py (extended by 02-02):
```python
DOMAIN = "party_dispenser"
LOGGER: Logger = getLogger(__package__)

CONF_HOST = "host"
CONF_PORT = "port"
CONF_JWT = "jwt"
CONF_USE_TLS = "use_tls"
CONF_SCAN_INTERVAL = "scan_interval"

DEFAULT_PORT = 8000
DEFAULT_SCAN_INTERVAL = 30
MIN_SCAN_INTERVAL = 5
MAX_SCAN_INTERVAL = 600
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create config_flow.py with PartyDispenserConfigFlow + OptionsFlowHandler (CFG-01, CFG-02, CFG-03)</name>
  <files>custom_components/party_dispenser/config_flow.py</files>
  <read_first>
    - /Users/jamaze/projects/hacs-integration-pd/.planning/phases/02-integration-core/02-RESEARCH.md (lines 1288-1545: full config_flow.py copy-ready content; lines 732-771: Pitfall 2 — Options flow 2025.12+ MUST NOT set self.config_entry in __init__; lines 140-140: TextSelector with TextSelectorConfig(type=PASSWORD) for JWT)
    - /Users/jamaze/projects/hacs-integration-pd/.planning/phases/02-integration-core/02-CONTEXT.md (lines 56-66: user step schema; lines 140-146: research-backed overrides #4 — NO self.config_entry in OptionsFlowHandler.__init__)
    - /Users/jamaze/projects/hacs-integration-pd/.planning/phases/02-integration-core/02-VALIDATION.md (lines 44-49: per-requirement test map for CFG-01/02/03 — lists the test IDs 02-04 will add)
    - /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/api.py (created by 02-02 — this task imports PartyDispenserApiClient + 4 exception classes from here)
    - /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/const.py (extended by 02-02 — this task imports CONF_HOST/CONF_PORT/CONF_JWT/CONF_USE_TLS/CONF_SCAN_INTERVAL/DEFAULT_PORT/DEFAULT_SCAN_INTERVAL/MIN_SCAN_INTERVAL/MAX_SCAN_INTERVAL/DOMAIN/LOGGER from here)
  </read_first>
  <action>
Create `custom_components/party_dispenser/config_flow.py` with the EXACT content from 02-RESEARCH.md lines 1288-1545. Copy the full file verbatim. Structure has 4 parts:

1. **Imports** (lines 1290-1327 of research): voluptuous, ConfigFlow/ConfigFlowResult/OptionsFlow from homeassistant.config_entries, callback from homeassistant.core, selector from homeassistant.helpers, async_get_clientsession from homeassistant.helpers.aiohttp_client; plus api.py exception classes + const.py config keys.

2. **`_user_schema(defaults)` helper** (lines 1330-1373): Returns a voluptuous Schema with 5 fields:
   - `CONF_HOST`: `selector.TextSelector(TextSelectorConfig(type=TEXT))`
   - `CONF_PORT`: `selector.NumberSelector(NumberSelectorConfig(min=1, max=65535, step=1, mode=BOX))` default `DEFAULT_PORT`
   - `CONF_JWT`: `selector.TextSelector(TextSelectorConfig(type=PASSWORD))` — REQUIRED, type=PASSWORD so the UI masks input
   - `CONF_USE_TLS`: `selector.BooleanSelector()` default `False`
   - `CONF_SCAN_INTERVAL`: `selector.NumberSelector(NumberSelectorConfig(min=MIN_SCAN_INTERVAL, max=MAX_SCAN_INTERVAL, step=1, mode=BOX, unit_of_measurement="s"))` default `DEFAULT_SCAN_INTERVAL`

3. **`_validate_connection(hass, host, port, use_tls, jwt)` helper** (lines 1378-1390): Builds `base_url = f"{http|https}://{host}:{port}"`, instantiates `PartyDispenserApiClient(base_url, jwt, session=async_get_clientsession(hass))`, calls `await client.list_recipes()`. Raises on any PartyDispenser*Error subclass — the caller translates to form errors.

4. **`PartyDispenserConfigFlow(ConfigFlow, domain=DOMAIN)` class** (lines 1395-1458):
   - `VERSION = 1`, `MINOR_VERSION = 1`
   - `async def async_step_user(self, user_input)`:
     - If `user_input is None`: show empty form with `_user_schema()`
     - Else: extract `host`, `port` (coerce via `int(user_input[CONF_PORT])`), set unique_id `f"{host}:{port}"` via `await self.async_set_unique_id(unique_id)` + `self._abort_if_unique_id_configured()`
     - Try `await _validate_connection(...)`
     - On `PartyDispenserAuthError`: `errors["base"] = "invalid_auth"`
     - On `PartyDispenserConnectionError`: `errors["base"] = "cannot_connect"`
     - On `PartyDispenserProtocolError`: `errors["base"] = "invalid_response"`
     - On `PartyDispenserError` (catch-all): `LOGGER.exception(...)`, `errors["base"] = "unknown"`
     - On success: `return self.async_create_entry(title=f"Party Dispenser ({host}:{port})", data={CONF_HOST, CONF_PORT, CONF_USE_TLS, CONF_JWT}, options={CONF_SCAN_INTERVAL: int(user_input.get(CONF_SCAN_INTERVAL, DEFAULT_SCAN_INTERVAL))})`
     - End of method: `return self.async_show_form(step_id="user", data_schema=_user_schema(user_input), errors=errors)`
   - `@staticmethod @callback def async_get_options_flow(config_entry) -> OptionsFlowHandler`: returns `OptionsFlowHandler()` — **NO ARGS** (per 02-RESEARCH.md Pitfall 2 lines 732-771).

5. **`OptionsFlowHandler(OptionsFlow)` class** (lines 1463-1544):
   - **CRITICAL: `__init__` takes NO args.** Do NOT write `def __init__(self, config_entry): self.config_entry = config_entry`. The parent OptionsFlow class provides `self.config_entry` via property backed by `self._config_entry_id`. Setting it manually is a hard error in HA 2025.12+ (we pin HA 2026.2.3). Omit `__init__` entirely or use `pass`.
   - `async def async_step_init(self, user_input)`:
     - Seed current values from `self.config_entry.data` (for jwt/use_tls) and `self.config_entry.options` (for scan_interval)
     - If `user_input is not None`: call `_validate_connection(self.hass, host=self.config_entry.data[CONF_HOST], port=self.config_entry.data[CONF_PORT], use_tls=bool(user_input.get(CONF_USE_TLS, False)), jwt=user_input[CONF_JWT])`
     - On success: mutate config entry data via `self.hass.config_entries.async_update_entry(self.config_entry, data={**self.config_entry.data, CONF_JWT: user_input[CONF_JWT], CONF_USE_TLS: bool(user_input.get(CONF_USE_TLS, False))})`; return `self.async_create_entry(title="", data={CONF_SCAN_INTERVAL: int(user_input.get(CONF_SCAN_INTERVAL, DEFAULT_SCAN_INTERVAL))})` — options ONLY hold scan_interval; JWT + TLS live in entry.data so they're available at setup time
     - On errors: same invalid_auth / cannot_connect / unknown mapping as user step
     - Schema: like _user_schema but WITHOUT host+port (they are NOT editable — per CONTEXT.md "Host and port are NOT editable after creation — force a remove-and-re-add if they change (the unique_id depends on them)")
     - Show form: `return self.async_show_form(step_id="init", data_schema=schema, errors=errors)`

**Pitfall guard — lint-compliant imports:**
- Keep `from homeassistant.core import callback` at module level (not inside TYPE_CHECKING) — it's used at decoration time
- `from homeassistant.config_entries import ConfigEntry` stays at module level (used as parameter type hint for `async_get_options_flow`)
- `from typing import Any` stays at module level

**Pitfall guard — ruff formatting:**
After writing the file, run:
```bash
cd /Users/jamaze/projects/hacs-integration-pd && ruff format . && ruff check .
```
`ruff format` normalizes whitespace/quotes/line breaks; `ruff check` validates imports (I-), naming (N-), and security (S-). Both MUST exit 0 before commit. Phase 1 Plan 03 burned a CI run on skipping this — bake it into the commit routine. (Lesson from 01-03-SUMMARY.md line 55.)

**Commit routine:**
```bash
cd /Users/jamaze/projects/hacs-integration-pd && ruff format . && ruff check .
git add custom_components/party_dispenser/config_flow.py
git commit -m "feat(02-01): add PartyDispenserConfigFlow + OptionsFlowHandler (CFG-01, CFG-02, CFG-03)"
```
  </action>
  <verify>
    <automated>cd /Users/jamaze/projects/hacs-integration-pd && ruff format --check . && ruff check . && python -c "from custom_components.party_dispenser.config_flow import PartyDispenserConfigFlow, OptionsFlowHandler; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exists
    - `grep -q "class PartyDispenserConfigFlow(ConfigFlow, domain=DOMAIN):" /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0
    - `grep -q "class OptionsFlowHandler(OptionsFlow):" /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0
    - `grep -q "async def async_step_user" /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0
    - `grep -q "async def async_step_init" /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0
    - `grep -q "_abort_if_unique_id_configured" /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0 (unique_id dedupe present)
    - `grep -q 'TextSelectorType.PASSWORD' /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0 (JWT is password-masked)
    - `grep -q 'invalid_auth' /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` AND `grep -q 'cannot_connect' /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` AND `grep -q 'invalid_response' /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` (all 3 error codes mapped)
    - `grep -q 'PartyDispenserApiClient' /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0 (imports api client for validation)
    - `grep -q 'async_get_clientsession' /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0 (uses HA shared session, not a fresh one)
    - `grep -q 'return OptionsFlowHandler()' /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` exits 0 — confirms OptionsFlowHandler() is called with NO args (2025.12+ pattern)
    - `grep -E "def __init__.*self.*config_entry.*:" /Users/jamaze/projects/hacs-integration-pd/custom_components/party_dispenser/config_flow.py` returns NOTHING (no OptionsFlowHandler.__init__ with config_entry arg — Pitfall 2 compliance)
    - `cd /Users/jamaze/projects/hacs-integration-pd && ruff format --check .` exits 0
    - `cd /Users/jamaze/projects/hacs-integration-pd && ruff check .` exits 0
    - `cd /Users/jamaze/projects/hacs-integration-pd && python -c "from custom_components.party_dispenser import config_flow"` exits 0 (module imports cleanly)
  </acceptance_criteria>
  <done>
config_flow.py exists with PartyDispenserConfigFlow (ConfigFlow subclass, domain=DOMAIN, single `async_step_user` step) and OptionsFlowHandler (OptionsFlow subclass, NO `__init__` args, single `async_step_init` step). User step collects all 5 fields, validates via PartyDispenserApiClient.list_recipes() with 10s timeout, maps exceptions to form errors (invalid_auth / cannot_connect / invalid_response / unknown), uses f"{host}:{port}" as unique_id to prevent dupes, creates entry with data={host, port, use_tls, jwt} + options={scan_interval}. OptionsFlowHandler re-validates on save, mutates entry.data via hass.config_entries.async_update_entry for jwt/use_tls changes, stores scan_interval in options. ruff format+check green. Tests are NOT in this plan — 02-04 adds `tests/test_config_flow.py` covering all 6 required scenarios (show_form, happy_path, invalid_auth, cannot_connect, duplicate_abort, options_flow_jwt_rotation) per 02-VALIDATION.md.
  </done>
</task>

</tasks>

<verification>
Phase-level checks executed after this plan merges:
- `ruff format --check . && ruff check .` green (enforced by CI lint stage)
- `python -c "from custom_components.party_dispenser.config_flow import PartyDispenserConfigFlow, OptionsFlowHandler"` succeeds
- `grep -c "TextSelectorType.PASSWORD" custom_components/party_dispenser/config_flow.py` == 2 (user step + options step both mask JWT)
- `grep "class PartyDispenserConfigFlow" custom_components/party_dispenser/config_flow.py` shows `domain=DOMAIN` kwarg (registers with HA)

Tests live in 02-04. After 02-04 lands, the full verification for this plan is:
- `pytest tests/test_config_flow.py -v` — 6 tests pass (show_form, happy_path, invalid_auth, cannot_connect, duplicate_abort, options_flow_jwt_rotation)
</verification>

<success_criteria>
CFG-01 (config flow collects host/port/JWT/TLS + validates connectivity before save): satisfied by `async_step_user` + `_validate_connection` helper.
CFG-02 (options flow rotates JWT without re-adding integration): satisfied by OptionsFlowHandler.async_step_init.
CFG-03 (no YAML edits required): satisfied structurally by `config_flow: true` in manifest.json (flipped by 02-02) + this config_flow.py making the UI flow functional.

Measurable completion: 2 classes (PartyDispenserConfigFlow, OptionsFlowHandler) exported from config_flow.py, both importable, lint-green, and the 6 pytest scenarios 02-04 will add (test_show_user_form, test_happy_path_creates_entry, test_invalid_auth_shows_error, test_cannot_connect_shows_error, test_duplicate_aborts, test_options_flow_jwt_rotation) are designed to pass against this implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-integration-core/02-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` — document the single task commit, note any deviations from the research file (none expected; content is copy-ready), flag that tests for this plan live in 02-04, and confirm the 2025.12+ OptionsFlowHandler pattern was followed (no `self.config_entry =` in `__init__`).
</output>
